# OpenAI API key (optional - for advanced natural language processing)
# Get from https://platform.openai.com/api-keys  
# The app works great without this using intelligent fallbacks!
OPENAI_API_KEY=your_openai_api_key_here

# Weather API: Uses Open-Meteo (completely FREE, no registration needed!)
# No API key required - the app automatically gets real weather data